# План выполнения — Станция техобслуживания

Живой документ. Обновляется по ходу всей работы: выполненные шаги отмечаются `[x]`, но **не удаляются** — так виден реальный ход работы (это тоже часть требуемого "хода работы во времени"). Если состав шагов меняется — новая запись с датой в разделе "Изменения плана" внизу, старые шаги не трогаем.

## Легенда приоритетов

- **A** — критически важные шаги. Без них проект не работает и не демонстрируется вообще (сквозной happy path).
- **B** — основные шаги. Обязательные пункты именно этого задания (12. Станция техобслуживания) и требования к "доказуемости" edge-cases.
- **C** — дополнительные шаги — внедрение собственной фичи (роботизация станции + ИИ-диагностика).
- **D** — полировка, исправления некритичного функционала.

Внутри грейда цифра = порядок выполнения (A1 выполняется раньше A2 и т.д.). Если время заканчивается — сначала жертвуем D, потом C, потом непроверенными частями B. Грейд A не режем.

## C1 — зафиксированный скоуп кастомной фичи

Чтобы "роботы + ИИ" не расползлись в неопределённый объём работы, фиксирую скоуп:

- **Робот** — программная симуляция на посту: очередь задач, состояния `свободен / выполняет / ошибка / готово`, длительность выполнения = длительность услуги из каталога, небольшой случайный шанс сбоя (для честной демонстрации обработки нештатной ситуации).
- **ИИ-модуль** — по профилю автомобиля (марка/модель/пробег/история обслуживания) и выполняемой услуге генерирует рекомендацию дополнительных работ с текстовым обоснованием. Реализация — pluggable: если задан ключ реального LLM API, вызывается модель; если нет — используется собственный rule-based генератор с тем же интерфейсом (проект остаётся быстро разворачиваемым локально без секретов, как требует общее условие про эмуляцию внешних сервисов).
- Робот и ИИ не заменяют человека-мастера — они автоматизируют то, что по базовому заданию 12 делает "мастер": выполнение работ и предложение доп. работ. Согласование доп. работы клиентом остаётся обязательным шагом (тоже требование задания).

## Список шагов

### Грейд A — критично (сквозной путь)

- [x] A1 — Инициализация репозитория: backend/, frontend/, docker-compose с PostgreSQL, health-check. **Стек и обоснование — см. `ARCHITECTURE.md`.** _(2026-09-10: скелет создан. Синтаксис backend проверен `py_compile`, JSON-конфиги frontend проверены. **Обновление 2026-09-10 (после установки Docker+WSL2): `docker compose up --build` реально проверен** — все три контейнера (`db`, `backend`, `frontend`) собрались и стартовали, `db` прошёл healthcheck, `backend` ответил `{"status":"ok"}` на `/health`, `frontend` отдал 200 на :5173.)_
- [x] A2 — Схема БД: Клиент, Автомобиль, Услуга (каталог), Пост, Заявка, статус заявки — миграции. _(2026-09-11: 5 моделей + связующая `booking_services`, статус заявки — нативный Postgres ENUM, индекс `(post_id, start_at)` под будущую проверку занятости слота (A4). Alembic-миграция `909e4725fc6d` сгенерирована автоматически и применена без ручных правок. Тест `test_schema_roundtrip` подтверждает запись/чтение со связями через реальную БД. Заодно поправил deprecation-предупреждение Pydantic в `config.py`.)_
- [x] A2.1 (внеплановое расширение схемы, см. "Изменения плана") — добавлены `TireSet` и `AdditionalWork` — сущности из B1/B2, перенесённые в схему заранее по прямой просьбе пользователя. _(2026-09-11: миграция `c1528f5a8b01`, тест расширен на обе сущности. Бизнес-логика (запрет повторной выдачи шин, поток согласования доп. работы) не реализована — это осталось за B1/B2 как и было.)_
- [x] Контрольная проверка A1/A9/A2/A2.1 — см. `VERIFICATION.md` + `scripts/verify.sh`. _(2026-09-11: нашёл и исправил реальный баг — `downgrade()` в автосгенерированных миграциях не дропал Postgres ENUM-типы, из-за чего цикл `downgrade → upgrade` падал. Исправлено в обеих миграциях. После фикса цикл `upgrade → downgrade → upgrade` проходит многократно; `alembic check` подтверждает отсутствие расхождений между моделями и БД; `alembic heads` — одна голова, миграции не ветвятся; тесты зелёные.)_
- [x] A3 — Backend: базовые CRUD для Клиент/Автомобиль/Каталог услуг. _(2026-09-11: роутеры `clients`/`cars`/`catalog`, Pydantic Create/Update/Read-схемы отдельно от ORM-моделей, 404 на несуществующий id, 409 на дубликат уникального поля через перехват `IntegrityError`, `POST /cars` дополнительно проверяет существование клиента. 9 тестов (CRUD + 404 + 409 по каждой сущности) зелёные. По пути найдены и исправлены 2 бага — см. "Изменения плана". Дополнительно лично подтверждено пользователем через Swagger UI в браузере — полный набор CRUD-операций по каждой таблице, с данными на русском и английском.)_
- [ ] A4 — Backend: расчёт свободных слотов по постам и длительности услуг + конкурентно-безопасное резервирование (транзакция/лок) — ядро проекта.
- [ ] A5 — Backend: конечный автомат статусов заявки (принята → на посту → ожидает согласования → готова → выдана).
- [ ] A6 — Realtime-канал (SSE) для трансляции статусов заявок и занятости постов всем открытым клиентам.
- [ ] A7 — Frontend: каркас (роутинг, страница записи, экран станции) + подключение к API + realtime без reload.
- [ ] A8 — Frontend: форма записи (услуги → свободные слоты → подтверждение) — happy path целиком.
- [x] A9 — `docker-compose up` разворачивает всё одной командой + README с предпосылками. _(2026-09-10: механизм подтверждён реальным запуском; README обновлён с инструкцией и портами. Формально A9 по плану шёл позже A2-A8 — отмечаю как выполненный именно в части "инфраструктура работает", а не "весь функционал есть".)_
- [ ] A10 — Скрипт-доказательство: два клиента одновременно бронируют один пост на одно время → один получает слот, другой — явный отказ.

### Грейд B — основные (обязательные пункты задания 12)

- [ ] B1 — Хранение шин: комплект привязан к клиенту+авто, сдача/выдача, запрет повторной или чужой выдачи. _(таблица `tire_sets` уже создана в A2.1 — здесь остаётся сама бизнес-логика и API, не схема.)_
- [ ] B2 — Согласование дополнительных работ: предложение доп. работы → уведомление клиенту (в интерфейсе + email-заглушка) → работа не начинается без ответа клиента.(Пометка пользователя: досогласование работ предлагается клиенту сразу при обнаружении возможности сделать дополнительные работы, чтобы съэкономить время, если клиент отвечает не сразу - продолжать выполнение согласованных работ) _(таблица `additional_works` уже создана в A2.1 — здесь остаётся сама бизнес-логика и API, не схема.)_
- [ ] B3 — Email-заглушка + напоминание клиенту за день до записи (ровно одно).
- [ ] B4 — Перенос/отмена записи: слот освобождается сразу, виден другим клиентам без reload.
- [ ] B5 — Проактивное предложение записи по пробегу/сроку следующего ТО(пометка пользователя: /количеству мест ближайшей записи, если оно ограничено остаётся 3 свободных места, клиенту предлагается запись, если у него подходит срок - обоснование - возможно потом мест для записи не будет.) (фоновая проверка).
- [ ] B6 — Сезонное напоминание по хранению шин — одно приглашение за сезон на клиента.
- [ ] B7 — Экран станции: заявки на день по постам, статус каждой, live-обновление.
- [ ] B8 — Тесты/скрипты-доказательства по edge-cases: двойная запись на пост, гонка при одновременном переносе/отмене, некорректные данные.
- [ ] B9 — Честное описание состояния проекта (что работает/что нет/почему) + раздел "что сделали бы следующим заходом".

### Грейд C — дополнительно (кастомная фича: роботизация + ИИ)

- [x] C1 — Зафиксирован скоуп фичи (см. раздел выше).
- [ ] C2 — Симулятор робота на посту: очередь задач, состояния, длительность = длительность услуги, случайный сбой с эскалацией.
- [ ] C3 — Живая трансляция прогресса выполнения клиенту и на экран станции (расширение канала из A6).
- [ ] C4 — ИИ-модуль диагностики доп. работ (LLM-вызов с фолбэком на rule-based генератор).
- [ ] C5 — Интеграция ИИ-рекомендаций в поток согласования доп. работ (B2) — предложение может создаваться автоматически.
- [ ] C6 — Сценарий-доказательство полного цикла: запись → робот выполняет базовую услугу → ИИ находит доп. работу → клиент подтверждает → робот доделывает → готово.

### Грейд D — полировка

- [ ] D1 — UI-полировка и адаптивность.
- [ ] D2 — Расширенное тестовое покрытие сверх ключевых сценариев.
- [ ] D3 — Логирование/отладочные метрики (не входят в требования, но полезны).
- [ ] D4 — Доп. фильтры/поиск в истории визитов — если останется время.
- [ ] D5 — Финальная полировка README/документации.

## Разбивка по 10 дням

Даты пока не привязаны к календарю — сначала нужно согласовать срок сдачи с MadDevs (см. `context/PROJECT_REQUIREMENTS.md`, п.5). Ниже — рабочие дни по ≈8 часов.

| День | Шаги | Цель дня |
|---|---|---|
| 1 | A1, A2 | Репозиторий, стек, схема БД |
| 2 | A3, A4 | CRUD + ядро логики бронирования слотов |
| 3 | A5, A6 | Статусная машина заявки + realtime-канал |
| 4 | A7, A8 | Frontend-каркас + форма записи, happy path работает целиком |
| 5 | A9, A10, B1 | Локальный запуск одной командой, доказательство конкурентности, хранение шин |
| 6 | B2, B3, B4 | Согласование доп. работ, email-заглушка+напоминание, перенос/отмена |
| 7 | B5, B6, B7 | Проактивные предложения ТО, сезонное напоминание по шинам, экран станции |
| 8 | B8, B9, C2 | Тесты edge-cases, честное описание состояния, старт симулятора робота |
| 9 | C3, C4 | Живой прогресс робота, ИИ-модуль диагностики |
| 10 | C5 | Интеграция ИИ-рекомендаций в поток согласования |

**⚠️ Не помещается в 10 дней:** C6 (финальный e2e-сценарий с роботом+ИИ) и весь грейд D (D1–D5) остаются за периметром 10-дневного плана — по текущей оценке трудозатрат это ещё **+1.5–2 дня** сверху (см. `context/SHORTLIST_COMPARISON.csv`: 11–12 дней на этот вариант целиком). Варианты:
1. Принять срок 11–12 дней и так и сообщить в MadDevs.
2. Урезать грейд C дальше (например, оставить C2–C4 без C5/C6, согласование доп. работ оставить полностью ручным).
3. Держать день 10 как чек-пойнт "готов MVP + дифференцирующая фича без финального интеграционного теста", и добить C6+D по возможности сверху, не блокируя сдачу.

Решение по этому пункту — на усмотрение пользователя, зафиксировать при согласовании даты с MadDevs.

## Контрольная проверка (2026-09-11)

Прогон на работоспособность + сверка с общими требованиями из `context/PROJECT_REQUIREMENTS.md` (п.2), на текущем состоянии проекта (после A1/A9, до A2).

| # | Требование | Статус | Комментарий |
|---|---|---|---|
| 1 | Клиент-серверная архитектура | ✅ | backend (FastAPI) и frontend (React) — раздельные процессы, общаются по HTTP |
| 2 | Backend Python/Go/Node | ✅ | Python 3.12 + FastAPI |
| 3 | Frontend Vue/React | ✅ | React + TypeScript |
| 4 | Реляционная СУБД, предпочтительно PostgreSQL | ✅ (инфраструктура) | контейнер `db` поднимается и проходит healthcheck; схемы/таблиц пока нет — это A2 |
| 5 | Учётные записи — на усмотрение | ⏳ не реализовано | запланировано, не блокирует остальное |
| 6 | Промпты/лог решений вместе с кодом | ✅ | `logs/SESSION_LOG.md`, `logs/DECISIONS_LOG.md` ведутся с первого дня |
| 7 | Использование AI-агента не скрывается | ✅ | зафиксировано в логах и README |
| 8 | Описаны предпосылки для развёртывания | ✅ | README, раздел "Как развернуть локально" |
| 9 | Быстрый локальный запуск | ✅ | `docker compose up --build` — проверено дважды (2026-09-10, 2026-09-11), сейчас исправно |
| 10 | Внешние сервисы можно эмулировать | ⏳ не реализовано | email-заглушка — шаг B3, ещё не сделан |
| 11 | Работает при двух одновременно открытых клиентах | ⏳ не проверяемо | нет ни одной realtime-фичи и бизнес-функциональности — это A6-A8; сейчас есть только статическая health-check страница |
| 12 | Честное описание состояния проекта | ✅ | README "Статус" + этот раздел |
| 13 | Доказуемость | ⚠️ частично | backend тест-сьют существовал, но был сломан (см. ниже) — исправлено; полноценных доказательств бизнес-логики пока нет, так как её пока нет (A2+) |

**Найден и исправлен баг:** `docker compose exec backend pytest` падал с `ModuleNotFoundError: No module named 'app'` — pytest без явного `pythonpath` добавлял в `sys.path` только `tests/`, а не корень `/app`, где лежит пакет `app`. Исправлено добавлением `pythonpath = .` в `backend/pytest.ini` (заодно убрал сопутствующее deprecation-предупреждение pytest-asyncio, добавив `asyncio_default_fixture_loop_scope = function`). После фикса: `1 passed`.

**Дополнительно проверено:**
- `docker compose ps` — все три контейнера `Up`, `db` — `healthy`.
- `GET /health` → `{"status":"ok"}`.
- `GET /` (frontend, :5173) → `200`.
- CORS preflight (`OPTIONS /health` с `Origin: http://localhost:5173`) → `access-control-allow-origin: http://localhost:5173` — корректно разрешает запросы с фронтенда.

**Вывод:** инфраструктурный скелет (A1/A9) работоспособен и был единственной сломанной частью — тестовый раннер, который лечится минимальным конфигом, а не изменением кода. Остальные ⏳-пункты — не баги, а честно ещё не реализованный функционал (A2 и далее по плану). Контейнеры остановлены после проверки (`docker compose down`).

## Изменения плана

_(записи добавлять сюда с датой, если состав шагов меняется; ничего из списка выше не удаляется)_

- `2026-09-11` — Пользователь попросил добавить в БД все сущности из описания Задания 12 в PDF сразу, не дожидаясь B1/B2. Добавлены `TireSet` и `AdditionalWork` (миграция `c1528f5a8b01`) с полями по ARCHITECTURE.md — вне очереди, как отдельный под-шаг A2.1. Сама бизнес-логика по ним осталась в B1/B2, как и планировалось — изменилась только очерёдность создания таблиц, не объём этих шагов.
- `2026-09-11` — При реализации A3 найдены и исправлены 2 бага, не связанных с бизнес-логикой: (1) FastAPI требует явный `response_model=None` на DELETE-роутах со статусом 204, иначе падает на старте с `AssertionError`; (2) `engine` в `db/session.py` использовал дефолтный пул asyncpg-соединений, который ломается при переиспользовании соединения между разными event loop — pytest-asyncio создаёт новый loop под каждый тест. Исправлено переходом на `NullPool`. Оба фикса — инфраструктурные, не меняют бизнес-требования шага.
- `2026-09-11` — Пользователь лично уточнил B2 (доп. работа предлагается клиенту сразу по обнаружении, при задержке ответа — продолжать согласованные работы) и B5 (триггер проактивного предложения записи — также по остатку свободных мест: если у ближайшей записи остаётся ≤3 мест и клиенту подходит срок, предложить запись, т.к. позже мест может не быть). Пометки внесены прямо в текст шагов B2/B5 выше.
