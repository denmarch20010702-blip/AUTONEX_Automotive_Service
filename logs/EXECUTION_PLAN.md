# План выполнения — Станция техобслуживания

Живой документ-чеклист для сверки дальнейших шагов и правок (дополнения/удаления шагов). Выполненные шаги отмечаются `[x]`, но **не удаляются** — так виден реальный ход работы во времени. Если состав шагов меняется — новая запись с датой в разделе "Изменения плана" внизу, старые шаги не трогаем.

**Разделение ответственности между логами (важно для поддержания порядка):** здесь — только факт, дата и статус шага, одной строкой. Подробности — что сломалось, как чинили, ход обсуждения с пользователем, находки багов, цифры тестов — живут в `DECISIONS_LOG.md` (хронология решений) и `SESSION_LOG.md` (детальный лог по промптам). Если аннотация к шагу тянет больше чем на 1-2 строки — это сигнал, что содержимое нужно перенести в `DECISIONS_LOG.md`, а здесь оставить только ссылку.

## Легенда приоритетов

- **A** — критически важные шаги. Без них проект не работает и не демонстрируется вообще (сквозной happy path).
- **B** — основные шаги. Обязательные пункты именно этого задания (12. Станция техобслуживания) и требования к "доказуемости" edge-cases.
- **C** — дополнительные шаги — внедрение собственной фичи (роботизация станции + ИИ-диагностика).
- **D** — полировка, исправления некритичного функционала.

Внутри грейда цифра = порядок выполнения (A1 выполняется раньше A2 и т.д.). Если время заканчивается — сначала жертвуем D, потом C, потом непроверенными частями B. Грейд A не режем.

## C1 — зафиксированный скоуп кастомной фичи

Чтобы "роботы + ИИ" не расползлись в неопределённый объём работы, фиксирую скоуп:

- **Робот** — программная симуляция на посту: очередь задач, состояния `свободен / выполняет / ошибка / готово`, длительность выполнения = длительность услуги из каталога, небольшой случайный шанс сбоя (для честной демонстрации обработки нештатной ситуации).
- **ИИ-модуль** — по профилю автомобиля (марка/модель/пробег/история обслуживания) и выполняемой услуге генерирует рекомендацию дополнительных работ с текстовым обоснованием. Реализация — pluggable: если задан ключ реального LLM API, вызывается модель; если нет — используется собственный rule-based генератор с тем же интерфейсом (проект остаётся быстро разворачиваемым локально без секретов, как требует общее условие про эмуляцию внешних сервисов).
- Робот и ИИ не заменяют человека-мастера — они автоматизируют то, что по базовому заданию 12 делает "мастер": выполнение работ и предложение доп. работ. Согласование доп. работы клиентом остаётся обязательным шагом (тоже требование задания).

## Список шагов

### Грейд A — критично (сквозной путь)

- [x] A1 — Инициализация репозитория: backend/, frontend/, docker-compose с PostgreSQL, health-check. **Стек и обоснование — см. `../buisness/ARCHITECTURE.md`.** _(2026-09-10, готово — `docker compose up --build` проверен, все 3 контейнера здоровы.)_
- [x] A2 — Схема БД: Клиент, Автомобиль, Услуга (каталог), Пост, Заявка, статус заявки — миграции. _(2026-09-11, готово — 5 моделей + `booking_services`, миграция `909e4725fc6d`.)_
- [x] A2.1 (внеплановое расширение схемы, см. "Изменения плана") — добавлены `TireSet` и `AdditionalWork`, перенесённые из B1/B2. _(2026-09-11, готово — миграция `c1528f5a8b01`; бизнес-логика по-прежнему в B1/B2.)_
- [x] Контрольная проверка A1/A9/A2/A2.1 — см. `../VERIFICATION.md` + `../scripts/verify.sh`. _(2026-09-11, пройдено — нашёл и исправил баг с недропаемыми Postgres ENUM в `downgrade()`, детали в `DECISIONS_LOG.md`.)_
- [x] A3 — Backend: базовые CRUD для Клиент/Автомобиль/Каталог услуг. _(2026-09-11, готово — 9 тестов; лично подтверждено пользователем через Swagger UI.)_
- [x] A4 — Backend: расчёт свободных слотов по постам и длительности услуг + конкурентно-безопасное резервирование — ядро проекта. _(2026-09-11, готово — `EXCLUDE USING gist` + `FOR UPDATE`, 25/25 тестов после серии находок и полного аудита; подробная история находок — в `DECISIONS_LOG.md`.)_
- [x] A5 — Backend: конечный автомат статусов заявки (принята → на посту → ожидает согласования → готова → выдана). _(2026-09-12, готово — 5 тестов, 30/30 всего.)_
- [x] A6 — Realtime-канал (SSE) для трансляции статусов заявок всем открытым клиентам. _(2026-09-12, готово — 35/35 тестов, живой стриминг вручную подтверждён для 2 одновременных клиентов.)_
- [x] A7 — Frontend: каркас (роутинг, страница записи, экран станции) + подключение к API + realtime без reload. _(2026-09-12, готово — визуально подтверждено пользователем в браузере.)_
- [x] A8 — Frontend: форма записи (услуги → свободные слоты → подтверждение) — happy path целиком. _(2026-09-12–13, готово и закрыто пользователем 2026-09-13 после нескольких итераций по обратной связи — мастер по UI-спеке, личный кабинет, статусы станции, выручка, журнал вместо удаления, фикс часового пояса и др. Полная хронология — в `DECISIONS_LOG.md`. Дальнейшая полировка сознательно отложена на B8/B9/D.)_
- [x] A9 — `docker-compose up` разворачивает всё одной командой + README с предпосылками. _(2026-09-10, готово.)_
- [x] A10 — Скрипт-доказательство: два клиента одновременно бронируют один пост на одно время → один получает слот, другой — явный отказ. _(2026-09-13, готово — `backend/scripts/prove_concurrency.py`, реальный HTTP к живому backend, стабильно `201`+`409` за 4 прогона подряд.)_

### Грейд B — основные (обязательные пункты задания 12)

- [x] B1 — Хранение шин: комплект привязан к клиенту+авто, сдача/выдача, запрет повторной или чужой выдачи. _(2026-09-13, готово — `POST /tire-sets`, `POST /tire-sets/{id}/issue`, `GET /tire-sets`; частичный уникальный индекс на 1 активный комплект/машину; UI в кабинете клиента и на станции. 5 тестов, 46/46 всего. Лично проверено пользователем в браузере.)_
- [x] B2 — Согласование дополнительных работ: предложение доп. работы → уведомление клиенту (в интерфейсе + email-заглушка) → работа не начинается без ответа клиента. _(2026-09-13, готово — `POST/GET /bookings/{id}/additional-works`, `POST /additional-works/{id}/respond` (approve/decline, 409 повторно); email-заглушка через новую таблицу `outbox_emails` (переиспользуется в B3/B5/B6); UI на станции (предложить + список) и в кабинете клиента (принять/отклонить); realtime через новые SSE-события. 5 тестов + 1 регресс на попутно найденный краш (см. `DECISIONS_LOG.md`), 55/55 всего. Уточнено 2026-09-13: пока есть неотвеченное предложение, `POST /bookings/{id}/status` блокирует все переходы кроме отмены (409) — статус реально не двигается без ответа клиента. Далее уточнено (та же дата): "ожидает согласования" теперь входит только при реальном pending-предложении и само выходит в "готова", как только клиент ответил на последнее — станции больше нечего нажимать вручную; согласованная доп. работа начисляется в выручку только при выдаче, сверх суммы услуги. 58/58 тестов. Детали — в `DECISIONS_LOG.md`.)_
- [x] B3 — Email-заглушка + напоминание клиенту за день до записи (ровно одно). _(2026-09-13, готово — `Booking.reminder_sent` + периодический опрос `app/services/reminders.py` (APScheduler, раз в 15 мин, окно 24ч); идемпотентность через флаг гарантирует ровно одно письмо в `outbox_emails`. 3 теста (в окне/не дублируется/не рано), 61/61 на тот момент.)_
- [x] B4 — Перенос/отмена записи: слот освобождается сразу, виден другим клиентам без reload. _(2026-09-12, отмена реализована. 2026-09-14, перенос доведён до конца: `POST /bookings/{id}/reschedule` — только из `accepted`, тот же набор услуг/машина, новое время проверяется той же логикой конкурентной защиты, что и создание (A4), с исключением самой заявки из проверки занятости; UI — компактный виджет выбора слота прямо в строке "Мои записи" кабинета. 5 тестов, 75/75 всего.)_
- [ ] B5 — Проактивное предложение записи по пробегу/сроку следующего ТО. _(Пометка пользователя: также по количеству мест ближайшей записи — если остаётся ≤3 свободных и клиенту подходит срок, предложить запись заранее, т.к. позже мест может не быть.)_
- [ ] B6 — Сезонное напоминание по хранению шин — одно приглашение за сезон на клиента.
- [x] (частично) B7 — Экран станции. _(2026-09-13: таблица заявок дополнена столбцами "Клиент" (имя+почта) и "Услуга" (изначально забронированная) по заметке пользователя п.12. 2026-09-14: `GET /station/actionable-count` (заявок в `accepted`/`ready`) — на нём держится красная точка у "Станция" в шапке (п.22). Разбивки по постам как отдельной доски пока нет — остаётся открытым.)_
- [x] B8 — Тесты/скрипты-доказательства по edge-cases: двойная запись на пост, гонка при одновременном переносе/отмене, некорректные данные. _(2026-09-13, готово по итогам целевого аудита существующего функционала — нашёл и исправил 2 реальных бага (регистр email, вводящая в заблуждение сумма в архиве для отменённых); 2 новых постоянных теста на гонки (отмена vs продвижение статуса, двойная выдача); 49/49 всего. Детали — в `DECISIONS_LOG.md`.)_
- [ ] B9 — Честное описание состояния проекта (что работает/что нет/почему) + раздел "что сделали бы следующим заходом".

### Грейд C — дополнительно (кастомная фича: роботизация + ИИ)

- [x] C1 — Зафиксирован скоуп фичи (см. раздел выше).
- [x] (частично) C2 — Симулятор робота на посту. _(2026-09-13, сделан пилотный кусок: автотаймер на `on_post` длительностью = сумма длительностей услуг, по истечении сам переводит в `awaiting_approval`/`ready` (APScheduler); проверено вживую реальным ожиданием 2 минуты. Обратный отсчёт до конца работ виден и станции, и клиенту (`service_ends_at` на заявке + `Countdown.tsx`, заметка пользователя п.11). Расширено (та же дата): согласованная доп. работа тоже запускает этот таймер (п.19) — заявка едет обратно "на пост" на её длительность вместо мгновенного "готова". Очередь задач и случайный сбой с эскалацией — не сделаны, остаются в C2.)_
- [ ] C3 — Живая трансляция прогресса выполнения клиенту и на экран станции (расширение канала из A6).
- [ ] C4 — ИИ-модуль диагностики доп. работ (LLM-вызов с фолбэком на rule-based генератор).
- [ ] C5 — Интеграция ИИ-рекомендаций в поток согласования доп. работ (B2) — предложение может создаваться автоматически.
- [ ] C6 — Сценарий-доказательство полного цикла: запись → робот выполняет базовую услугу → ИИ находит доп. работу → клиент подтверждает → робот доделывает → готово.

**Ниже — фичи из `buisness.md` (AUTONEX), добавлены 2026-09-11. Порядок учитывает зависимости** (каждая опирается на данные/статусы из предыдущих шагов), **не только важность** — поэтому не всегда совпадает с рейтингом из `BUSINESS_FEATURES_REVIEW.md`.

- [ ] C7 — Smart Parking Management. Сущность `ParkingSpot` (id, номер). Занятость — простое занято/свободно + `occupied_at` (не диапазон времени — момент выхода заранее не известен). Автоназначение места по переходу заявки в `ready` (A5). 2 часа на оплату+выезд, после — наценка за простой. **Скоуп-допущение (решение пользователя, 2026-09-11):** свободное место есть всегда (постов 3, мест 6) — сценарий "мест нет" сознательно не обрабатывается.
- [ ] C8 — Real-time AUTONEX Control Center (расширение B7): посты + парковка + агрегированные счётчики через SSE (A6). Зависит от C7.
- [ ] C9 — AI Service Scheduler: расширяет A4 — учитывает тип услуги/авто и текущую загрузку станции. Зависит от A4, C7.
- [ ] C10 — Autonomous Vehicle Mode + оплата: флаг "беспилотный" на `Car`, платёжная заглушка, автовыезд для беспилотных. Зависит от C7.
- [ ] C11 — Automatic Invoice: агрегированный счёт из услуг, доп. работ, диагностики, наценки за простой. Зависит от B2, C4, C7, C10.
- [ ] C12 — Customer Dashboard: кабинет клиента — авто, статус обслуживания, место на парковке. Зависит от A3, A5, C7.
- [ ] C13 — Customer Rating & AI Feedback Analysis: `Rating` (booking_id, оценка, комментарий) + ИИ-сводка тем. Зависит только от A5.

### Грейд D — полировка

- [x] (частично) D1 — UI-полировка. _(2026-09-13, разобраны 10 находок пользователя из ручного тестирования — `../buisness/UI_description.md`: редактирование услуги, автоподсказки марки/модели авто, кнопка "в кабинет" после записи, фикс "неудаляемой" услуги, стрелки смены даты — на края экрана без кружка, разрыв бесконечного цикла дата⇄авто при конфликте конкретной машины. Детали и что не тронуто — в `DECISIONS_LOG.md`. Адаптивность (мобильная вёрстка) не проверялась — открыто.)_
- [x] (частично, продолжение) D1 — переход станции/кабинета с голых HTML-таблиц и серых `link-button` на единый визуальный язык из спеки (бледно-зелёные `.panel`/`.action-button`/`.ghost-button`, стилизованные таблицы) + ещё 5 находок пользователя (п.11-15: таймер виден клиенту и станции, столбцы клиент/услуга в таблице заявок, убрано фиктивное ручное "согласование", доп. работы начисляются при выдаче, стрелки дальше от края с более широкой зоной клика). _(2026-09-13, готово — 58/58 бэкенд-тестов, `tsc -b` чист. Детали — в `DECISIONS_LOG.md`.)_
- [x] (частично, продолжение) D1 — ещё 4 находки пользователя (п.16-19): 2 старые "зависшие" issued-заявки (№14/136, тесты A5 от 2026-09-12 до появления архивации) вручную доархивированы с согласия пользователя, выручка досчитана; кнопки "Изменить"/"Удалить" на плитке авто больше не выпирают за края; доп. работа выбирается кликом из каталога услуг, а не вводится вручную, согласованная — запускает настоящий таймер выполнения (см. C2); красная точка-уведомление у "Личный кабинет" (пока есть неотвеченное предложение доп. работы) и у "Станция" (новая заявка/смена статуса, гаснет при заходе). _(2026-09-13, готово — 64/64 бэкенд-тестов, `tsc -b` чист. Детали — в `DECISIONS_LOG.md`.)_
- [x] (частично, продолжение) D1 — 11 находок пользователя (п.20-30): найдены и исправлены 6 реальных багов — доп. работа, одобренная после того как основная услуга уже готова, теперь тоже запускает таймер выполнения (п.20/25, раньше не запускался вообще); комплект шин теперь архивируется при выдаче (новая `TireSetArchive`, как и у заявок), поэтому больше не блокирует навечно удаление машины/клиента ложным "активно" (п.28); нельзя сохранить машину/услугу с пустым названием (п.26/30) или отрицательным пробегом; нельзя менять данные машины при активной записи (п.27); длина названия услуги ограничена валидацией + CSS-подстраховкой для старых данных (п.24). Добавлены фичи: клиент сам меняет своё имя/почту и может удалить свой аккаунт из кабинета (п.21/29); напоминание "запись завтра" теперь видно и в самом кабинете, не только в email-заглушке (п.23); красная точка у "Станция" переведена с "погасло от захода на страницу" на реальный счётчик заявок, ждущих решения (accepted/ready), и продублирована прямо в строке заявки/доп. работы, где нужно решение (п.22). Всё лично проверено пользователем в браузере — подтверждено. _(2026-09-14, готово — 75/75 бэкенд-тестов, `tsc -b` чист. Детали — в `DECISIONS_LOG.md`.)_
- [x] (частично, продолжение) D1 — п.31: дата последнего ТО машины теперь сама обновляется на дату завершения работ при выдаче заявки, видна в кабинете клиента. _(2026-09-14, готово — 76/76 бэкенд-тестов.)_
- [ ] D2 — Расширенное тестовое покрытие сверх ключевых сценариев. _(Включает фронтенд-тесты (Vitest) для чистых функций с реальным потенциалом бага — `bucketForHour`/`shiftDate`/`localMidnightIso`, сейчас проверенных только вручную. Сейчас 0 фронтенд-тестов при 41 backend-тесте.)_
- [ ] D3 — Логирование/отладочные метрики (не входят в требования, но полезны).
- [ ] D4 — Доп. фильтры/поиск в истории визитов — если останется время.
- [ ] D5 — Финальная полировка README/документации.
- [ ] D6 — CI: GitHub Actions, прогоняющий backend-тесты (и позже `tsc -b`/фронтенд-тесты) на каждый push/PR — сейчас проверка только ручная.
- [ ] D7 — Гигиена зависимостей: закоммитить `frontend/package-lock.json` (сейчас нет, версии не зафиксированы точно — в отличие от `backend/requirements.txt`, где уже `==`); добавить линтер на обе стороны (ruff/eslint), подключить в D6.
- [ ] D8 — Корневой `AGENTS.md` — единая точка входа для агента/ревьюера: как поднять/проверить проект, где искать план/архитектуру/журналы, что можно менять свободно. Не дублирует существующие документы — короткий файл-указатель на них.

## Разбивка по дням

Даты пока не привязаны к календарю — сначала нужно согласовать срок сдачи с MadDevs (см. `../context/PROJECT_REQUIREMENTS.md`, п.5). Ниже — рабочие дни по ≈8 часов.

| День | Шаги | Цель дня |
|---|---|---|
| 1 | A1, A2 | Репозиторий, стек, схема БД |
| 2 | A3, A4 | CRUD + ядро логики бронирования слотов |
| 3 | A5, A6 | Статусная машина заявки + realtime-канал |
| 4 | A7, A8 | Frontend-каркас + форма записи, happy path работает целиком |
| 5 | A9, A10, B1 | Локальный запуск одной командой, доказательство конкурентности, хранение шин |
| 6 | B2, B3, B4 | Согласование доп. работ, email-заглушка+напоминание, перенос/отмена |
| 7 | B5, B6, B7 | Проактивные предложения ТО, сезонное напоминание по шинам, экран станции |
| 8 | B8, B9, C2 | Тесты edge-cases, честное описание состояния, старт симулятора робота |
| 9 | C3, C4 | Живой прогресс робота, ИИ-модуль диагностики |
| 10 | C5 | Интеграция ИИ-рекомендаций в поток согласования |
| 11 | C6, C7 (старт) | Доказательство базового цикла робот+ИИ; сущность `ParkingSpot` и её занятость |
| 12 | C7 (финиш) | Автоназначение места, грейс-период, наценка за простой |
| 13 | C8 | Control Center: посты + парковка + счётчики на SSE |
| 14 | C9 | AI Service Scheduler: выбор поста/слота с учётом типа услуги/авто и загрузки |
| 15 | C10 | Autonomous Vehicle Mode + платёжная заглушка + автовыезд |
| 16 | C11 | Automatic Invoice (агрегация услуг + доп. работ + диагностики + парковки) |
| 17 | C12 | Customer Dashboard (кабинет клиента) |
| 18 | C13 | Customer Rating & AI Feedback Analysis |
| 19 | D1–D8 | Полировка, CI, гигиена зависимостей, финальная документация |

**Как режем, если время не хватит** (от конца к началу, грейд A не трогаем никогда):
1. Сначала весь грейд D (день 19) — полировка, без неё проект всё равно "доказуем".
2. Затем C13 → C12 → C11 → C10 → C9 → C8 → C7 (от менее к более критичным по `BUSINESS_FEATURES_REVIEW.md`) — каждую можно убрать целиком без переделки уже сделанного.
3. C6 не резать — единственное прямое доказательство работы дифференцирующей фичи (роботизация+ИИ).
4. Грейд B резать нельзя — обязательные пункты самого Задания 12.

Решение по итоговому сроку — на усмотрение пользователя, зафиксировать при согласовании даты с MadDevs (открытый вопрос — в `logs/DECISIONS_LOG.md`).

## Контрольная проверка (2026-09-11, ~11:28–11:40)

Прогон на работоспособность + сверка с общими требованиями из `../context/PROJECT_REQUIREMENTS.md` (п.2), на состоянии проекта после A1/A9, до A2.

| # | Требование | Статус | Комментарий |
|---|---|---|---|
| 1 | Клиент-серверная архитектура | ✅ | backend (FastAPI) и frontend (React) — раздельные процессы, общаются по HTTP |
| 2 | Backend Python/Go/Node | ✅ | Python 3.12 + FastAPI |
| 3 | Frontend Vue/React | ✅ | React + TypeScript |
| 4 | Реляционная СУБД, предпочтительно PostgreSQL | ✅ (инфраструктура) | контейнер `db` здоров; схемы — это A2 |
| 5 | Учётные записи — на усмотрение | ⏳ | запланировано, не блокирует остальное |
| 6 | Промпты/лог решений вместе с кодом | ✅ | `logs/SESSION_LOG.md`, `logs/DECISIONS_LOG.md` ведутся с первого дня |
| 7 | Использование AI-агента не скрывается | ✅ | зафиксировано в логах и README |
| 8 | Описаны предпосылки для развёртывания | ✅ | README, раздел "Как развернуть локально" |
| 9 | Быстрый локальный запуск | ✅ | `docker compose up --build` проверено |
| 10 | Внешние сервисы можно эмулировать | ⏳ | email-заглушка — B3, ещё не сделан |
| 11 | Работает при двух одновременно открытых клиентах | ⏳ | нет ещё бизнес-функциональности — A6-A8 |
| 12 | Честное описание состояния проекта | ✅ | README "Статус" + этот раздел |
| 13 | Доказуемость | ⚠️ частично | тест-сьют был сломан, исправлено; бизнес-логики пока нет (A2+) |

Найден и исправлен баг: `pytest` в контейнере не резолвил пакет `app` — исправлено `pythonpath = .` в `pytest.ini`. Инфраструктурный скелет (A1/A9) работоспособен; остальные ⏳-пункты — честно ещё не реализованный функционал, не баги.

## Изменения плана

_(Только факт изменения состава/скоупа шагов, одной-двумя строками. Находки багов и ход обсуждения — в `DECISIONS_LOG.md`.)_

- `[2026-09-11]` B2/B5 — пользователь уточнил критерии приёмки; внесено прямо в текст шагов выше.
- `[2026-09-11]` A2.1 добавлен вне очереди — `TireSet`/`AdditionalWork` перенесены из B1/B2 в схему заранее.
- `[2026-09-11]` A4 — дизайн-решение: фиксированный интервал занятости поста (не "по событию"), скоуп A4 не изменился.
- `[2026-09-11]` Добавлены C7–C13 (фичи AUTONEX) с порядком по зависимостям; разбивка по дням расширена с 10 до 19.
- `[2026-09-11]` Из бронирования убраны `post_id`/`end_at` — оба выводятся автоматически (auto-assign поста, сумма длительностей услуг).
- `[2026-09-11]` Добавлен `GET /bookings` — не было в исходном скоупе A4.
- `[2026-09-12]` B4 частично перенесён вперёд — отмена (не перенос) реализована вместе с личным кабинетом.
- `[2026-09-13]` Часовой пояс расчёта суток для слотов — сознательно привязан к устройству клиента для демо (решение пользователя), не к станции. Путь к правильному поведению в релизе — в `buisness/ARCHITECTURE.md`.
- `[2026-09-13]` A8 закрыт по решению пользователя — дальнейшая полировка отложена на B8/B9/D.
- `[2026-09-13]` Добавлены D6–D8 (CI, гигиена зависимостей+линтеры, `AGENTS.md`) по итогам анализа аудитов из `pravki/` (гигиена-отчёты по другому проекту того же работодателя) — применимое перенесено, неприменимое (релизный процесс, OSS-чеклист, hash-checked install, защита CSV-экспорта) осознанно не перенесено как нерелевантное приватному тестовому репозиторию.
- `[2026-09-13]` Пользователь лично внёс 10 находок из ручного тестирования в `../buisness/UI_description.md` — разобраны, применимое закрыто в рамках D1/B2/C2 выше (детали — в `DECISIONS_LOG.md`).
- `[2026-09-13]` Пользователь дописал в `../buisness/UI_description.md` ещё 5 находок (п.11-15) и попросил параллельно довести весь интерфейс станции/кабинета до единого стиля спеки (не только таблицы и серые ссылки) — разобрано и закрыто в рамках D1/B2/B7/C2 выше.
- `[2026-09-13]` Пользователь дописал в `../buisness/UI_description.md` ещё 4 находки (п.16-19) и попросил перейти к B3, продолжая разбирать документ — B3 реализован, находки закрыты в рамках D1/C2 выше (детали — в `DECISIONS_LOG.md`).
- `[2026-09-14]` Пользователь попросил довести B4 до конца (был отмечен частично выполненным) и дописал в `../buisness/UI_description.md` ещё 11 находок (п.20-30) — B4 (перенос) реализован полностью, находки закрыты в рамках D1/B7 выше (детали — в `DECISIONS_LOG.md`).
- `[2026-09-14]` Пользователь дописал п.31 (готово, см. D1 выше) и п.32 — уточнение п.29 (самоудаление аккаунта должно каскадно удалять машины/записи, но блокироваться, если есть шины на хранении) — пользователь сам отметил это как некритичное уточнение на будущее, реализация отложена без явной команды.
